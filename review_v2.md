1. red-before-green phải có ordering/fingerprint
2. mutation phải verify runner + threshold
3. LEVELS phải có sub-level/check ID để gate enforce đầy đủ
4. evidence phải bind với case/spec definition, không chỉ code